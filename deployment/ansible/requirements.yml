---
# Ansible Galaxy roles required for deployment
# Install with: ansible-galaxy install -r requirements.yml

roles:
  - name: ansistrano.deploy
    src: ansistrano.deploy
    version: '4.4.0'

  - name: ansistrano.rollback
    src: ansistrano.rollback
    version: '4.0.1'
