# Preprod secrets — copy to secrets.yml and fill in real values
# secrets.yml is gitignored
#
# ansible_user is the SSH user used to connect and deploy.
# It must have write access to deploy_to and permission to restart the service.

ansible_user: "<preprod-deploy-user>"
ansible_become: true
ansible_become_method: sudo
