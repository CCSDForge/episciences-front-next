# Production inventory — copy to production.ini and fill in real values
# production.ini is gitignored

[production]
prod-front-01 ansible_host=<VM1_IP_OR_HOSTNAME>
# prod-front-02 ansible_host=<VM2_IP_OR_HOSTNAME>

[production:vars]
ansible_port=22
